language: node_js

node_js:
  - "13.6.0"

services:
  - docker
  
install:
  - npm install

after_success:
  - docker build -t $DOCKER_IMAGE .

deploy:
  provider: script
  script: docker push $DOCKER_IMAGE
  on:
    branch: development